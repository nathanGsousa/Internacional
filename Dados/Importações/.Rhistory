by = "sh4",
all.x = TRUE
)
# diagnóstico simples
sem_secao <- dt[is.na(secao), .N]
cat("\nAno:", ano, "- linhas sem seção:", sem_secao, "\n")
# manter apenas o necessário
dt <- dt[, .(
secao,
nome_secao,
co_pais_ison_num,
periodo,
valor_fob
)]
# agregação final no formato útil para o CMS
dt <- dt[, .(
valor_fob = sum(valor_fob, na.rm = TRUE)
), by = .(
secao,
nome_secao,
co_pais_ison_num,
periodo
)]
# guardar ano para conferência
dt[, ano := ano]
return(dt)
}
anos <- 2015:2024
pasta_baci <- "C:/Users/Nathan/Home/Internacional/Dados/Importações"
lista_baci <- lapply(
anos,
function(a) processa_baci_ano(
ano = a,
pasta_base = pasta_baci,
sh4_tab = sh4_tab
)
)
baci_todos_anos <- rbindlist(lista_baci, use.names = TRUE, fill = TRUE)
dim(baci_todos_anos)
head(baci_todos_anos)
str(baci_todos_anos)
# consolidar os 10 anos em apenas 2 períodos
baci_cms_ref <- baci_todos_anos[, .(
valor_fob = sum(valor_fob, na.rm = TRUE)
), by = .(
secao,
nome_secao,
co_pais_ison_num,
periodo
)]
# conferir
dim(baci_cms_ref)
head(baci_cms_ref)
str(baci_cms_ref)
baci_cms_ref[, .N, by = periodo]
baci_cms_ref[, .(
valor_total = sum(valor_fob, na.rm = TRUE)
), by = periodo]
baci_cms_ref[, .(
n_destinos = uniqueN(co_pais_ison_num)
), by = periodo]
baci_cms_ref[, .(
n_secoes = uniqueN(secao)
), by = periodo]
fwrite(
baci_cms_ref,
"C:/Users/Nathan/Home/Internacional/Dados/Importações/baci_cms_ref.csv",
sep = ";"
)
require(dplyr)
require(tidyr)
require(readr)
require(stringr)
exportacao_ne <- read_delim(
"C:/Users/Nathan/Home/Internacional/Dados/Exportação/Dados_ex_cms.csv",
delim = ";",
locale = locale(encoding = "Latin1"),
show_col_types = FALSE
)
exportacao_ne <- read_delim(
"C:/Users/Nathan/Home/Internacional/Dados/Exportações/Dados_ex_cms.csv",
delim = ";",
locale = locale(encoding = "Latin1"),
show_col_types = FALSE
)
demanda_mundial <- read_delim(
"C:/Users/Nathan/Home/Internacional/Dados/Importação/baci_cms_ref.csv",
delim = ";",
locale = locale(encoding = "Latin1"),
show_col_types = FALSE
)
exportacao_ne <- read_delim(
"C:/Users/Nathan/Home/Internacional/Dados/Exportações/Dados_ex_cms.csv",
delim = ";",
locale = locale(encoding = "Latin1"),
show_col_types = FALSE
)
demanda_mundial <- read_delim(
"C:/Users/Nathan/Home/Internacional/Dados/Importação/baci_cms_ref.csv",
delim = ";",
locale = locale(encoding = "Latin1"),
show_col_types = FALSE
)
demanda_mundial <- read_delim(
"C:/Users/Nathan/Home/Internacional/Dados/Importações/baci_cms_ref.csv",
delim = ";",
locale = locale(encoding = "Latin1"),
show_col_types = FALSE
)
exportacao_ne <- read_delim(
"C:/Users/Nathan/Home/Internacional/Dados/Exportações/Dados_ex_cms.csv",
delim = ";",
locale = locale(encoding = "Latin1"),
show_col_types = FALSE
)
glimpse(exportacao_ne)
glimpse(demanda_mundial)
glimpse(exportacao_ne)
glimpse(demanda_mundial)
names(exportacao_ne)
names(demanda_mundial)
#Limpando
```
exportacao_ne <- read_delim(
"C:/Users/Nathan/Home/Internacional/Dados/Exportação/Dados_ex_cms.csv",
delim = ";",
locale = locale(encoding = "Windows-1252"),
show_col_types = FALSE
)
exportacao_ne <- read_delim(
"C:/Users/Nathan/Home/Internacional/Dados/Exportações/Dados_ex_cms.csv",
delim = ";",
locale = locale(encoding = "Windows-1252"),
show_col_types = FALSE
)
glimpse(exportacao_ne)
exportacao_ne <- read_delim(
"C:/Users/Nathan/Home/Internacional/Dados/Exportações/Dados_ex_cms.csv",
delim = ";",
locale = locale(encoding = "UTF-8"),
show_col_types = FALSE
)
glimpse(exportacao_ne)
glimpse(demanda_mundial)
glimpse(exportacao_ne)
glimpse(demanda_mundial)
names(exportacao_ne)
names(demanda_mundial)
glimpse(exportacao_ne)
glimpse(demanda_mundial)
#Limpando
exportacao_ne <- exportacao_ne %>%
mutate(
co_pais = as.character(co_pais),
co_pais_ison_num = as.integer(co_pais_ison_num),
periodo = str_trim(periodo),
valor_fob = as.numeric(valor_fob)
)
demanda_mundial <- demanda_mundial %>%
mutate(
co_pais_ison_num = as.integer(co_pais_ison_num),
periodo = str_trim(periodo),
valor_fob = as.numeric(valor_fob)
)
#Limpando
exportacao_ne <- exportacao_ne %>%
mutate(
co_pais = as.character(co_pais),
co_pais_ison_num = as.integer(co_pais_ison_num),
periodo = str_trim(periodo),
valor_fob = as.numeric(valor_fob)
)
demanda_mundial <- demanda_mundial %>%
mutate(
co_pais_ison_num = as.integer(co_pais_ison_num),
periodo = str_trim(periodo),
valor_fob = as.numeric(valor_fob)
)
##Checar estrutura
glimpse(exportacao_ne)
glimpse(demanda_mundial)
str(exportacao_ne)
str(demanda_mundial)
x_t <- exportacao_ne %>%
group_by(estado, periodo) %>%
summarise(
X_t = sum(valor_fob, na.rm = TRUE),
.groups = "drop"
)
x_t
View(x_t)
x_it <- exportacao_ne %>%
group_by(estado, secao, nome_secao, periodo) %>%
summarise(
X_it = sum(valor_fob, na.rm = TRUE),
.groups = "drop"
)
x_it
setwd("C:/Users/Nathan/Home/Internacional/Dados/Importações")
require(dplyr)
require(tidyr)
require(readr)
require(data.table)
require(stringr)
sh4_tab <- fread("C:/Users/Nathan/Home/Internacional/Dados/auxiliar/NCM_SH.csv", encoding = "Latin-1")
setDT(sh4_tab)
# ---------- tabela SH4 -> seção ----------
sh4_tab <- unique(
sh4_tab[, .(
sh4 = sprintf("%04d", as.integer(CO_SH4)),
secao = as.character(CO_NCM_SECROM),
nome_secao = NO_SEC_POR
)]
)
# conferir
head(sh4_tab)
str(sh4_tab)
baci_2024 <- fread(
"C:/Users/Nathan/Home/Internacional/Dados/BACI_HS12_Y2024_V202601.csv",
select = c("j", "k", "v"),
colClasses = list(character = c("j", "k"), numeric = "v"),
showProgress = TRUE
)
View(x_it)
# X_j,t = exportações do estado por destino no período
x_jt <- exportacao_ne %>%
group_by(estado, co_pais_ison_num, destino, periodo) %>%
summarise(
X_jt = sum(valor_fob, na.rm = TRUE),
.groups = "drop"
)
x_jt
# X_ij,t = exportações do estado por seção e destino no período
x_ijt <- exportacao_ne %>%
group_by(estado, secao, nome_secao, co_pais_ison_num, destino, periodo) %>%
summarise(
X_ijt = sum(valor_fob, na.rm = TRUE),
.groups = "drop"
)
x_ijt
View(x_jt)
View(x_ijt)
# r = crescimento percentual das importações mundiais entre os períodos 1 e 2
mundo_total <- demanda_mundial %>%
group_by(periodo) %>%
summarise(
valor_fob = sum(valor_fob, na.rm = TRUE),
.groups = "drop"
)
mundo_total_wide <- mundo_total %>%
pivot_wider(
names_from = periodo,
values_from = valor_fob
) %>%
mutate(r = (P2 / P1) - 1)
mundo_total
mundo_total_wide
# r_i
mundo_i <- demanda_mundial %>%
group_by(secao, nome_secao, periodo) %>%
summarise(
valor_fob = sum(valor_fob, na.rm = TRUE),
.groups = "drop"
)
mundo_i_wide <- mundo_i %>%
pivot_wider(
names_from = periodo,
values_from = valor_fob,
values_fill = 0
) %>%
mutate(r_i = if_else(P1 > 0, (P2 / P1) - 1, NA_real_))
mundo_i
mundo_i_wide
# r_i_j
mundo_ij <- demanda_mundial %>%
group_by(secao, nome_secao, co_pais_ison_num, periodo) %>%
summarise(
valor_fob = sum(valor_fob, na.rm = TRUE),
.groups = "drop"
)
mundo_ij_wide <- mundo_ij %>%
pivot_wider(
names_from = periodo,
values_from = valor_fob,
values_fill = 0
) %>%
mutate(r_ij = if_else(P1 > 0, (P2 / P1) - 1, NA_real_))
mundo_ij
mundo_ij_wide
View(mundo_total)
View(mundo_total_wide)
setwd("C:/Users/Nathan/Home/Internacional/Dados/Importações")
require(dplyr)
require(tidyr)
require(readr)
require(data.table)
require(stringr)
sh4_tab <- fread("C:/Users/Nathan/Home/Internacional/Dados/auxiliar/NCM_SH.csv", encoding = "Latin-1")
setDT(sh4_tab)
# ---------- tabela SH4 -> seção ----------
sh4_tab <- unique(
sh4_tab[, .(
sh4 = sprintf("%04d", as.integer(CO_SH4)),
secao = as.character(CO_NCM_SECROM),
nome_secao = NO_SEC_POR
)]
)
# conferir
head(sh4_tab)
str(sh4_tab)
processa_baci_ano <- function(ano, pasta_base, sh4_tab) {
arquivo <- sprintf("%s/BACI_HS12_Y%d_V202601.csv", pasta_base, ano)
dt <- fread(
arquivo,
select = c("j", "k", "v"),
colClasses = list(character = c("j", "k"), numeric = "v"),
showProgress = TRUE
)
setDT(dt)
# país importador do BACI = co_pais_ison_num
dt[, co_pais_ison_num := as.integer(j)]
# SH6 -> SH4
dt[, hs6 := sprintf("%06d", as.integer(k))]
dt[, sh4 := substr(hs6, 1, 4)]
# período
dt[, periodo := ifelse(ano >= 2015 & ano <= 2019, "P1", "P2")]
# valor: BACI está em thousand USD
dt[, valor_fob := v * 1000]
# manter só o necessário
dt <- dt[, .(
co_pais_ison_num,
sh4,
periodo,
valor_fob
)]
# agregar cedo
dt <- dt[, .(
valor_fob = sum(valor_fob, na.rm = TRUE)
), by = .(co_pais_ison_num, sh4, periodo)]
# join com SH4 -> seção
dt <- merge(
dt,
sh4_tab,
by = "sh4",
all.x = TRUE
)
# diagnóstico simples
sem_secao <- dt[is.na(secao), .N]
cat("\nAno:", ano, "- linhas sem seção:", sem_secao, "\n")
# manter apenas o necessário
dt <- dt[, .(
secao,
nome_secao,
co_pais_ison_num,
periodo,
valor_fob
)]
# agregação final no formato útil para o CMS
dt <- dt[, .(
valor_fob = sum(valor_fob, na.rm = TRUE)
), by = .(
secao,
nome_secao,
co_pais_ison_num,
periodo
)]
# guardar ano para conferência
dt[, ano := ano]
return(dt)
}
anos <- 2015:2024
pasta_baci <- "C:/Users/Nathan/Home/Internacional/Dados/Importações"
lista_baci <- lapply(
anos,
function(a) processa_baci_ano(
ano = a,
pasta_base = pasta_baci,
sh4_tab = sh4_tab
)
)
baci_todos_anos <- rbindlist(lista_baci, use.names = TRUE, fill = TRUE)
dim(baci_todos_anos)
head(baci_todos_anos)
str(baci_todos_anos)
fwrite(
baci_todos_anos,
"C:/Users/Nathan/Home/Internacional/Dados/Importações/baci_imp_anual.csv",
sep = ";"
)
setwd("C:/Users/Nathan/Home/Internacional/Dados/Importações")
require(dplyr)
require(tidyr)
require(readr)
require(data.table)
require(stringr)
sh4_tab <- fread("C:/Users/Nathan/Home/Internacional/Dados/auxiliar/NCM_SH.csv", encoding = "Latin-1")
setDT(sh4_tab)
# ---------- tabela SH4 -> seção ----------
sh4_tab <- unique(
sh4_tab[, .(
sh4 = sprintf("%04d", as.integer(CO_SH4)),
secao = as.character(CO_NCM_SECROM),
nome_secao = NO_SEC_POR
)]
)
# conferir
head(sh4_tab)
str(sh4_tab)
baci_2024 <- fread(
"C:/Users/Nathan/Home/Internacional/Dados/BACI_HS12_Y2024_V202601.csv",
select = c("j", "k", "v"),
colClasses = list(character = c("j", "k"), numeric = "v"),
showProgress = TRUE
)
setwd("C:/Users/Nathan/Home/Internacional/Dados/Importações")
require(dplyr)
require(tidyr)
require(readr)
require(data.table)
require(stringr)
sh4_tab <- fread("C:/Users/Nathan/Home/Internacional/Dados/auxiliar/NCM_SH.csv", encoding = "Latin-1")
setDT(sh4_tab)
# ---------- tabela SH4 -> seção ----------
sh4_tab <- unique(
sh4_tab[, .(
sh4 = sprintf("%04d", as.integer(CO_SH4)),
secao = as.character(CO_NCM_SECROM),
nome_secao = NO_SEC_POR
)]
)
# conferir
head(sh4_tab)
str(sh4_tab)
processa_baci_ano <- function(ano, pasta_base) {
arquivo <- sprintf("%s/BACI_HS12_Y%d_V202601.csv", pasta_base, ano)
dt <- fread(
arquivo,
select = c("j", "k", "v"),
colClasses = list(character = c("j", "k"), numeric = "v"),
showProgress = TRUE
)
setDT(dt)
# país importador do BACI = co_pais_ison_num
dt[, co_pais_ison_num := as.integer(j)]
# SH6 -> SH4
dt[, hs6 := sprintf("%06d", as.integer(k))]
dt[, sh4 := substr(hs6, 1, 4)]
# ano e período
dt[, ano := ano]
dt[, periodo := fifelse(ano >= 2015 & ano <= 2019, "P1", "P2")]
# valor: BACI está em thousand USD
dt[, valor_fob := v * 1000]
# manter só o necessário
dt <- dt[, .(
sh4,
co_pais_ison_num,
ano,
periodo,
valor_fob
)]
# agregação anual final
dt <- dt[, .(
valor_fob = sum(valor_fob, na.rm = TRUE)
), by = .(
sh4,
co_pais_ison_num,
ano,
periodo
)]
return(dt)
}
anos <- 2015:2024
pasta_baci <- "C:/Users/Nathan/Home/Internacional/Dados/Importações"
lista_baci <- lapply(
anos,
function(a) processa_baci_ano(
ano = a,
pasta_base = pasta_baci
)
)
baci_todos_anos <- rbindlist(lista_baci, use.names = TRUE, fill = TRUE)
# conferir
dim(baci_todos_anos)
head(baci_todos_anos)
str(baci_todos_anos)
setcolorder(
baci_todos_anos,
c("sh4", "co_pais_ison_num", "ano", "periodo", "valor_fob")
)
baci_cms_ref <- baci_todos_anos[, .(
valor_fob = sum(valor_fob, na.rm = TRUE)
), by = .(
sh4,
co_pais_ison_num,
periodo
)]
# conferir
dim(baci_cms_ref)
head(baci_cms_ref)
str(baci_cms_ref)
fwrite(
baci_todos_anos,
"C:/Users/Nathan/Home/Internacional/Dados/Importações/baci_imp_anual_sh4.csv",
sep = ";"
)
fwrite(
baci_cms_ref,
"C:/Users/Nathan/Home/Internacional/Dados/Importações/baci_cms_ref_sh4.csv",
sep = ";"
)
View(baci_cms_ref)
View(baci_todos_anos)
